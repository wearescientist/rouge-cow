# 肉鸽牛牛 - 完整设计大纲 v1.0

## 一、核心概念

### 1.1 游戏定位

| 维度 | 定义 |
|------|------|
| **类型** | 动作Roguelike + 幸存者like |
| **视角** | 2D俯视角，单屏幕房间制 |
| **核心循环** | 探索→战斗→成长→选择→重复 |
| **参考** | 以撒的结合（地图）+ 吸血鬼幸存者（战斗）+ Risk of Rain（道具叠加）|

### 1.2 独特卖点
- **"一切都是叠加的"** - 100个道具无上限堆叠，无负面协同
- **"一个屏幕一个世界"** - 以撒式房间，清理即通关，无拖泥带水
- **"牛牛宇宙"** - 感染动物主题，黑色幽默画风

---

## 二、核心系统架构

### 2.1 三层架构

```
┌─────────────────────────────────────┐
│         表现层 (Render)              │
│   Canvas 2D | 粒子系统 | 动画状态机   │
├─────────────────────────────────────┤
│         逻辑层 (Logic)               │
│  房间管理 | 战斗系统 | 道具系统       │
│  敌人AI   | 碰撞检测 | 存档系统       │
├─────────────────────────────────────┤
│         数据层 (Data)                │
│  道具数据库 | 武器配置 | 敌人属性      │
│  地图生成算法 | 存档序列化            │
└─────────────────────────────────────┘
```

### 2.2 系统依赖关系

```
玩家(Player)
 ├── 道具系统(ItemSystem) ← 道具数据库(100 items)
 ├── 武器系统(WeaponSystem) ← 武器配置(8 weapons + evolution)
 └── 属性计算(Stats)

房间(Room)
 ├── 地图生成(MapGenerator) ← BFS算法
 ├── 敌人管理(EnemyManager) ← 22 enemy types
 └── 障碍物(Obstacles)

游戏循环(GameLoop)
 ├── 更新(Update) → 实体更新 → 碰撞检测
 └── 绘制(Render) → 相机 → UI
```

---

## 三、详细机制设计

### 3.1 房间系统（以撒模式）

#### 房间类型

| 类型 | 颜色标识 | 敌人数 | 奖励 | 出现概率 |
|------|----------|--------|------|----------|
| **起点** | 绿色 | 0 | 无 | 固定1个 |
| **普通** | 灰色 | 5-10 | 1道具 | 60% |
| **宝箱** | 金色 | 0 | 2-3道具选择 | 15% |
| **商店** | 蓝色 | 0 | 购买道具 | 10% |
| **Boss** | 红色 | 1大+5小 | 传说道具 | 每层1个 |
| **隐藏** | 紫色 | 精英怪 | 诅咒道具 | 5% |

#### 门机制
- **锁定状态**：房间未清理时门关闭（红色X）
- **开启条件**：击杀所有敌人
- **传送逻辑**：玩家走进门→黑屏0.3秒→新房间中心生成

---

### 3.2 战斗系统

#### 武器模板

```javascript
Weapon = {
  id: string,
  name: string,
  damage: number,        // 基础伤害
  cooldown: number,      // 攻击间隔(秒)
  type: 'melee'|'projectile'|'orbit'|'area',
  
  // 特殊属性
  pierce: number,        // 穿透次数
  homing: boolean,       // 追踪
  bounce: number,        // 弹跳次数
  explosion: number,     // 爆炸半径
  
  // 进化系统
  evolution: {
    requires: [itemId],  // 需要的道具
    result: weaponId,    // 进化后武器
    bonus: { ... }       // 属性加成
  }
}
```

#### 8基础武器 + 进化

| 基础 | 类型 | 特点 | 进化条件 | 进化后 |
|------|------|------|----------|--------|
| 鞭子 | 近战 | 扇形范围 | hollowHeart | 血泪(吸血) |
| 魔杖 | 弹道 | 追踪 | emptyTome | 神圣魔杖(无CD) |
| 飞刀 | 弹道 | 穿透 | bracer | 千刃(无限穿透) |
| 斧头 | 弹道 | 回旋 | candelabrador | 死亡螺旋(3斧头) |
| 圣经 | 环绕 | 防御 | spellbinder | 不洁晚祷(永久) |
| 火球 | 弹道 | 爆炸 | spinach | 地狱火(大爆炸) |
| 闪电 | 瞬发 | 连锁 | duplicator | 雷霆循环(更多连锁) |
| 圣水 | 区域 | 持续伤害 | attractorb | 撒拉逊之水(追踪) |

---

### 3.3 道具系统（Risk of Rain模式）

#### 道具结构

```javascript
Item = {
  id: number (1-100),
  name: string,
  icon: emoji,
  rarity: 'common'|'rare'|'epic'|'legendary'|'cursed',
  
  effect: string,        // 属性名
  value: number,         // 基础值
  stackType: 'linear'|'hyperbolic'|'exponential'|'step',
  maxStacks: number      // 0=无限
}
```

#### 叠加公式

| 类型 | 公式 | 用途 |
|------|------|------|
| **Linear** | `value × count` | 伤害+10% |
| **Hyperbolic** | `1 - (1-value)^count` | 暴击率(防止100%) |
| **Exponential** | `(1+value)^count - 1` | 复利增长 |
| **Step** | count≥3?2×:1× | 质变阈值 |

#### 道具分类（100个分配）

```
攻击类 (25个)
├── 子弹数量: 多重射击、霰弹扩散...
├── 子弹质量: 巨大化、穿甲弹、暴击...
└── 特效: 冰冻、燃烧、中毒、连锁...

防御类 (25个)
├── 生命: 心之容器、再生因子...
├── 防御: 钢铁护甲、荆棘护甲...
└── 闪避: 闪避靴、相位移动...

移动类 (15个)
├── 速度: 加速靴、时间加速...
└── 穿越: 飞行翅膀、穿墙术...

资源类 (20个)
├── 金币: 金币袋、贪婪之手...
└── 掉落: 掉落加成、幸运币...

召唤类 (10个)
└── 随从: 小精灵、守护球、宠物龙...

诅咒类 (5个)
└── 双面刃: 狂暴模式、献祭之心...
```

---

### 3.4 敌人系统

#### 敌人属性

```javascript
Enemy = {
  id: string,
  name: string,
  hp: number,
  speed: number,
  damage: number,
  exp: number,
  
  behavior: 'chase'|'flee'|'orbit'|'stationary',
  abilities: ['dash', 'shoot', 'split', 'shield']
}
```

#### 22种敌人（4级难度）

| 等级 | 代表 | HP | 速度 | 特点 |
|------|------|----|------|------|
| **Tier 1** | 感染小鸡、老鼠、蜗牛 | 6-15 | 15-60 | 基础怪，无特殊 |
| **Tier 2** | 兔子、小鸟、鸽子 | 12-18 | 50-70 | 快速，会闪避 |
| **Tier 3** | 猫咪、鸭子、松鼠 | 20-28 | 45-75 | 会扑击、冲刺 |
| **Tier 4** | 狗、猪、绵羊 | 35-50 | 35-60 | 高血量，会击退 |
| **精英** | 鹅、熊、蛇 | 30-70 | 35-80 | 特殊技能(毒/眩晕) |
| **Boss** | 螃蟹、乌龟 | 100-120 | 20-25 | 全屏技能、召唤 |

#### AI行为树

```
Root
├── 状态检查
│   ├── 冻结? → 停止
│   ├── 眩晕? → 停止 + 计时器
│   └── 燃烧? → 持续掉血
├── 行为选择
│   ├── 距离>200? → ChasePlayer
│   ├── 距离<50?  → Attack + Retreat
│   └── 周围敌人>3? → Separation(分离)
└── 移动执行
    ├── 寻路: 直接朝向玩家
    ├── 避障: 简单推开
    └── 边界: 钳制在房间内
```

---

## 四、进度与成长系统

### 4.1 玩家属性

```javascript
Player = {
  // 基础
  hp: number,
  maxHp: number,
  speed: number,
  
  // 资源
  level: number,
  exp: number,
  gold: number,
  
  // 装备
  weapons: [Weapon × 6],  // 最大6把武器
  items: [Item × ∞],       // 道具无上限
  
  // 计算属性(实时)
  damageMult: number,
  fireRate: number,
  critChance: number,
  magnetRange: number
}
```

### 4.2 升级曲线

| 等级 | 需要EXP | 奖励 |
|------|---------|------|
| 1→2 | 100 | +1 HP |
| 2→3 | 200 | +1 HP, +10% 伤害 |
| 3→4 | 300 | +1 HP, 选择新武器/道具 |
| n→n+1 | n×100 | 每5级额外+1武器槽 |

### 4.3 难度缩放

```
波次难度 = 基础值 × (1 + 波数 × 0.1)

敌人属性缩放:
- HP: 100% + 波数 × 10%
- 伤害: 100% + 波数 × 5%
- 速度: 100% + 波数 × 2%
- 数量: 基础 + 波数 × 0.5
```

---

## 五、技术实现规范

### 5.1 类结构

```
Entity (基类)
 ├── Player
 ├── Enemy
 ├── Bullet
 └── Particle

Manager (管理器)
 ├── ItemManager (道具)
 ├── WeaponManager (武器)
 ├── RoomManager (房间)
 └── ParticleSystem (粒子)

Game (主控)
 ├── init()      // 初始化
 ├── update(dt)  // 逻辑更新
 ├── draw()      // 渲染
 └── loop()      // 主循环
```

### 5.2 性能优化点

| 优化项 | 方案 | 目标 |
|--------|------|------|
| 碰撞检测 | 空间哈希(Spatial Hash) | O(n) vs O(n²) |
| 粒子系统 | 对象池(Object Pool) | 零GC分配 |
| 渲染 | 脏矩形检测 | 只重绘变化区域 |
| 存档 | JSON序列化 + LocalStorage | <100ms保存 |

### 5.3 代码规范

- **坐标系**: 左上角(0,0)，右下(900,600)
- **时间单位**: 秒(float)，非毫秒
- **角度**: 弧度制，0=右，π/2=下
- **命名**: camelCase，类名PascalCase
- **错误处理**: 所有除法前检查0，所有数组访问检查边界

---

## 六、内容清单

### 6.1 已实现 ✅

- [x] 核心引擎(Entity, Vec2, 碰撞)
- [x] 房间系统(BFS生成，6种类型)
- [x] 道具系统(12核心道具，叠加公式)
- [x] 武器系统(4基础武器)
- [x] 敌人系统(基础AI，6种敌人)
- [x] 粒子系统(对象池)
- [x] 全屏自适应

### 6.2 待开发 🚧

- [ ] 完整100道具
- [ ] 武器进化系统(8→16)
- [ ] 完整22敌人(含Boss AI)
- [ ] 音效系统(Web Audio API)
- [ ] 存档系统(LocalStorage)
- [ ] 商店系统(金币消费)
- [ ] 成就系统
- [ ] 移动端触摸控制

### 6.3 未来扩展 🌟

- [ ] 多人联机(WebRTC)
- [ ] 创意工坊(自定义道具)
- [ ] 每日挑战(固定种子)
- [ ] 无尽模式
- [ ] 牛牛皮肤系统

---

## 七、文件结构

```
rougelike-cow/
├── index.html              # 主入口
├── src/
│   ├── core/
│   │   ├── engine.js       # 实体、向量、基础类
│   │   ├── particle.js     # 粒子系统
│   │   └── utils.js        # 工具函数
│   ├── systems/
│   │   ├── items.js        # 道具数据库+管理器
│   │   ├── weapons.js      # 武器系统
│   │   ├── enemies.js      # 敌人AI
│   │   └── rooms.js        # 房间+地图生成
│   └── game.js             # 主游戏类
├── assets/
│   └── sprites/            # 22个敌人精灵
└── doc/
    └── design.md           # 本大纲
```

---

## 八、快速参考

### 8.1 常用数值

| 常量 | 值 | 说明 |
|------|----|----|
| 屏幕尺寸 | 900×600 | 逻辑分辨率 |
| 玩家速度 | 150 | 像素/秒 |
| 子弹速度 | 200-350 | 像素/秒 |
| 敌人生成间隔 | 2秒 | 每波间隔 |
| 转场时间 | 0.3秒 | 黑屏过渡 |

### 8.2 快捷键

| 按键 | 功能 |
|------|------|
| WASD | 移动 |
| 1-9 | 获得对应ID道具 |
| 0 | 随机道具 |
| W | 获得随机武器 |
| F | 切换全屏 |
| ESC | 暂停 |

### 8.3 调试命令

```javascript
// 浏览器控制台可用
game.player.hp = 999        // 无敌
game.items.add(1)           // 获得道具
game.weaponManager.add('whip')  // 获得武器
```

---

*文档版本: v1.0*
*最后更新: 2026-02-18*
