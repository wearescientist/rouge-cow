# REPORT_HiddenRoom_Cinematic_Art_Overhaul_2026-03-19

## 变更摘要

- 收紧六层隐藏房的中心构图，统一把主体压回房间中心可视区。
- 强化二层黑兔演示击杀流程的镜头感与可读性。
- 重做六层母亲遗产房的桌面静物构图与桌体表现。
- 增加统一的电影质感叠层，包括暗角、空气雾、中心光场。
- 进一步收紧隐藏房光源尺度，降低玩家焦点光圈，改为环境微光主导。
- 一层改为中心舞台式模板，不再使用左右黑板块。
- 二层改为 20 条统一虫体，仅 4 条红色变异虫作为目标，黑兔改为发光示范体。

## 影响范围

- `src/systems/HiddenRoomSystem.js`

## 结果

- 通过 `node --check` 语法自检。
- 当前实现不依赖新增桌子贴图，六层桌体继续由代码绘制。

## 补充调整

- 一层将左右 4x2 操作区放大并外扩，改为更明确的中央水晶球 + 舞台式双区布局。
- 二层将蠕虫本体整体压暗并加软边，红色变异体仅保留颜色差异。
- 二层黑兔补充可见发光层，避免演示阶段主体过黑。
- 一层左侧模板图改为清晰单体蘑菇，不再使用综合式蓝菌素材。
- 一层左右操作区继续扩宽，减少中间堆叠感，水晶球抬回视觉中心。

## 本轮修正

- `RoomBlurSystem` 改为以透明挖黑幕为主，去掉高强度白色 reveal / clarity 洗亮路径。
- 隐藏房不再同时走 `HiddenRoomSystem` 场景级 light blob 与统一光场两条路径，避免重复打光。
- 一层模板区改为仅在“需要点亮”的格子绘制 `dec_mushroom`，无蘑菇格只保留极弱地面痕迹。
- 一层左右操作区取消矩形底板感，保留中央水晶球和双操作区的直接构图关系。

## 验证

- `node --check` 已通过：
  - `src/render/systems/RoomBlurSystem.js`
  - `src/systems/HiddenRoomSystem.js`
  - `src/systems/Room.js`
  - `src/systems/rooms/Room.js`
- 已运行一轮本地静态页 + Playwright 页面级验证，生成截图：`output/web-game-lighting/shot-0.png`
- 自动化验证目前只覆盖主菜单，未直接进入隐藏房；隐藏房最终观感仍需实机截图复核。

## 08:33 紧急修正

- 隐藏房场景下取消统一光场的模糊层输出，避免玩家与关键交互体被整层二次糊化。
- 回调隐藏房玩家焦点 carve 强度，恢复主体可读性，不再出现人物/水晶球“半透明发虚”。
- 一层环境装饰 alpha 回升，避免整体“透明化”。
- 一层蜡烛节点缩小绘制尺寸并移除暗色 tint，降低蜡烛区块化背景感。
- 二层蠕虫移除 `screen + blur` 硬高光路径，改为低强度软荧光与细轮廓，压掉白边违和感。
