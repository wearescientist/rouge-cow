# REPORT_UI_EntryFlow_Refresh_v0.33

## 日期
2026-03-08

## 本轮范围
- 统一首屏入口流视觉语言
- 覆盖 `加载页 / 设置弹窗 / 武器选择`
- 不触碰战斗逻辑与核心渲染链路

## 完成内容
- 将加载页改为卡片式居中面板，补充状态眉标、进度元信息与更稳定的提示区
- 将设置弹窗改为与主菜单同系的玻璃化暗面板，强化分组、标题层级与返回按钮状态
- 重做武器选择卡片的排版结构，增加图标徽章、说明尾注与选中反馈
- 保持原有 DOM id 与主要交互入口不变，降低与现有逻辑耦合风险

## 风险控制
- 未改动游戏内数值、存档、音频参数结构
- 武器选择仍走既有 `onSelectWeapon()` 回调，只追加视觉状态
- 设置面板仍由 `SettingsController` 注入高级选项，避免在 HTML 中重复维护逻辑

## 后续建议
- 第二轮继续收口战斗内 HUD，重点处理左右侧栏信息密度和顶部状态条层级
- 若继续强化入口演出，可为加载页补轻微粒子或门扉过渡，但应避免抢过开场动画的戏份
