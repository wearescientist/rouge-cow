This package contains the main code and lightweight patched weapon sprites only.
Excluded on purpose: most assets/, generated_assets/, node_modules/, dist/, build/.
If you need the full runtime art assets, use the full-with-assets package or copy your local assets back in.
